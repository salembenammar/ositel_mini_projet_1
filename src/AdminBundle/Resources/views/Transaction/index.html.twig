{#{% extends 'AdminBundle::layout.html.twig' %}#}
{% extends '@Admin/layout.html.twig' %}

{% block screen_content %}
<p>
    <a class="btn btn-secondary disabled" href="#" role="button">Ecran 1</a>
    <a class="btn btn-secondary" href="{{ path('transaction_screen_2') }}" role="button">Ecran 2</a>
</p>
{% endblock %}

{% block main_content %}
<a class="btn btn-primary float-right" href="{{ path('transaction_add') }}" role="button" style="color: white">Ajouter une transaction</a>

<br>
<br>

<table id="example" class="table table-striped table-bordered dt-responsive nowrap" style="width:100%">
    <thead>
    <tr>
        <th>Titre</th>
        <th>Description</th>
        <th>Montant</th>
        <th>Catégorie</th>
        <th>In/Out</th>
        <th>Date création</th>
        <th>Action</th>
    </tr>
    </thead>
    <tbody>
    </tbody>
</table>

<script>
    $(document).ready(function() {
        $('#example').DataTable( {
            language: {
                url: "https://cdn.datatables.net/plug-ins/1.10.16/i18n/French.json"
            },
            ajax: '{{ path("transaction_list") }}'
        } );
    } );
</script>
{% endblock %}